# Prism 领域语境

Prism 是一个 Markdown 桌面编辑器（Tauri 2 + React + TypeScript），产品定位为 Typora 式的单文档单窗口体验。本文档记录领域专家级别的语言与核心决策，供 AI 协作者与后续维护者对齐。

## 术语表

### Prototype（设计原型）
历史上指 `docs/prism-openai-redesign.html`。该原型不再是当前视觉与交互的唯一参考标准，只作为早期重构参考保留。

原先根目录的 `prism.html`（Win11 Fluent Design 方向）已于 2026-05-10 被取代，不再作为实现依据；文件现已归档到 `docs/archive/dirty-data-2026-05-30/history/prism.html`。

### 设计方向（Design language）
**妙言风格（Miaoyan-style）**。当前 Prism 的主视觉应贴近妙言式 macOS Markdown 写作器：内容优先、轻量原生桌面质感、克制浅色界面、低对比边框、紧凑工具区、舒适中文长文排版。后续 UI 优化必须以当前 app 的妙言风格为主，不再按 OpenAI 极简原型扩展新视觉。

OpenAI 原型中的部分原则仍可作为辅助约束：克制、留白、少装饰、少强色块。但不能继续套用纯黑白双锚点、强 pill 体系或大号 display 排印作为唯一判断标准。

### 单文档单窗口（Single-document-per-window）
Prism 没有标签页。每个窗口只打开一个文档，新开文档 = 新开窗口（`Ctrl+Shift+N`）。侧边栏的文件树是导航入口，不是标签容器。

此决策已明确，早期存在的 `TabBar` 组件属于旧方向的遗留。

### 写作器增强型（Writing-tool-first）
Prism 的主形态是单文档 Markdown 写作器；链接、模板、属性、快速打开、检查和导出都必须服务当前写作流程，而不是把 Prism 变成知识库工作台或 Notion 替代品。

### 视图模式（View mode）
三态互斥：`edit`（源码）/ `split`（分栏）/ `preview`（预览）。主入口在标题栏右上角的 `ViewModeSwitch` 图标组；菜单"视图"中作为 `checkable` 项呈现。状态栏不承载视图模式切换。

### 专注模式（Focus mode）
`F8` 触发。侧栏、菜单栏、标题栏 `opacity: 0.25`，鼠标 hover 恢复。不同于全屏 `F11`。

### 工作区（Workspace）
侧栏展示的根目录。一次打开一个工作区；`status-folder` 显示其名称，右键/点击弹出工作区级上下文菜单。

### 打字机模式（Typewriter mode）
`F9` 触发。开启后当前编辑行始终保持在 viewport 垂直中心。实现依赖 CodeMirror 6 的 `selection` 变化监听 + `scrollIntoView`。与专注模式**正交**，可同时打开。

### 恢复快照（Recovery snapshot）
用于异常退出、保存失败或冲突后的防丢稿安全副本；不是用户主动浏览和回滚的版本历史。

### 版本历史（Version history）
用户可主动浏览、比较、回滚的时间线式历史版本；Prism 当前不把它作为产品能力。

### 状态栏 zone（Status bar zones）
当前状态栏由两段式结构组成：
- `status-sidebar-zone` —— 仅当 `sidebarVisible=true` 时渲染，宽度对齐侧栏 260px。默认 `opacity: 0`，鼠标进入整段区域时淡入为 1（用于工作区快捷操作）。
- `status-main` —— 常驻。中区绝对定位的字数与行列信息；右区保留现有导出按钮与专注模式按钮，并显示 `ERROR n` 与后台导出状态。

状态栏优化只改编辑区下方的底部状态栏，不改变左侧边栏、文件树、标题栏、视图切换和已有全局入口，也不移除现有导出按钮与专注模式按钮。状态栏不是功能入口收纳区；除既有图标按钮外，只保留字数、行列、`ERROR n`、后台导出状态。

保存状态不放在底部状态栏。`已保存`、`已编辑未保存`、保存失败等文档脏状态由左上角文件名区域表达，底部不重复显示。

字数与行列用紧凑文本居中显示，格式为 `1,248 字 · 42:8`，其中 `42:8` 表示 `行:列`。不显示 `Words`、`Line`、`Col` 等标签；hover tooltip 可说明 `字数 · 行:列`。没有文档时不显示统计；只读预览模式如果没有光标，隐藏行列，仅显示字数。

YAML Front Matter、outlinks/backlinks、导出字段依赖等文档管理信息不放在底部状态栏；不显示 `META` / `LINK n` / `BACKLINK n`，也不新增文档信息图标入口。第一版先进入命令面板：`打开文档属性`、`查看当前文档链接`、`查看反向链接`；打开后用轻量弹窗/面板展示，反向链接条目可点击跳转。只有当这些能力被证明是高频入口时，再设计正式的文档信息面板。

状态栏中的诊断类入口统一聚合为 `ERROR n`，不再平铺 `LINK n` / `BACKLINK n` / `TYPO n` 等多个文本入口。

`ERROR n` 只统计需要用户处理的诊断问题：断链、缺失图片或无效图片路径、Mermaid/KaTeX 渲染失败、导出阻断问题、标题锚点冲突或跳转目标不存在。Backlink/outlink 数量属于导航信息，不计入错误；普通中文排版建议默认不计入错误，除非用户开启严格检查；Front Matter 缺字段属于建议，只有在当前导出模板强依赖该字段时才升级为错误。点击 `ERROR n` 打开统一诊断面板，按链接、图片、渲染、导出、排版分组。

诊断面板从 `ERROR n` 上方弹出，不做大 modal，也不新增右侧常驻栏。面板宽度约 360-420px，使用当前妙言风格的轻量桌面浮层：低对比背景、细边框、轻阴影、8px 内圆角以内、紧凑列表、hover 才强化可点击状态。标题显示 `n 个问题`，问题按严重程度排序；每条包含类型、简短描述、位置或行号、可选操作。点击问题跳转到对应位置。无错误时不显示 `ERROR n`。

后台导出状态只在有导出任务或最近导出结果时显示。导出中显示朴素 spinner + `导出中`，不显示假进度条；成功显示 `已导出` 并约 2 秒后消失；失败显示 `导出失败` 并保留，点击打开失败详情。点击 `导出中` 可重新打开导出 toast 或后台任务详情。

专注模式与打字机模式不在底部显示状态文字。现有专注模式按钮保留原位置和原交互；开启/关闭时可使用轻量 toast 或菜单 check 状态反馈，不新增常驻状态标签。

### 浮动工具栏（Floating toolbar）
选中文本后在鼠标选区上方弹出的 pill 形药丸工具栏。编辑器为 CodeMirror 6，位置通过 `view.coordsAtPos(selection.main.from)` 计算，非原型里的 textarea `lineHeight * row` 估算。按钮序列：B / I / U / S / highlight / code / link / quote。

### 斜杠菜单（Slash menu）
`/` 是编辑器正文里的 Markdown 原生命令入口，只在源码编辑区触发，不在预览区触发。它使用贴合当前 Prism 妙言风格的轻量菜单，不做 Notion 式大块卡片，也不引入数据库、看板、日历或 AI 生成块。

第一版只提供写作常用的 Markdown/HTML 片段插入：标题、表格、代码块、Mermaid、KaTeX、Callout、Toggle、分割线、图片、链接、模板。选择后插入标准 Markdown 或 HTML 片段，源码仍然可读、可编辑、可导出。菜单支持键盘上下选择、Enter 插入、Esc 关闭，并支持输入过滤，例如 `/mer` 过滤到 Mermaid。

### Callout 与 Toggle
Callout 使用 Obsidian/GitHub 风格的块引用语法，保持源码可读：

```md
> [!NOTE]
> 这里是提示内容
```

第一版支持 `NOTE`、`TIP`、`WARNING`、`IMPORTANT`。预览渲染为轻量提示块，不做彩色大卡片。

Toggle 使用原生 HTML `details/summary`，以保证 Markdown 源码可读并提升导出兼容性：

```html
<details>
<summary>标题</summary>

折叠内容

</details>
```

导出 HTML/PDF/DOCX 时应尽量保持预览效果，不引入私有 block 语法或数据库式结构。

### 模板系统（Templates）
模板系统第一版只做本地 Markdown 模板插入，不做模板市场、在线模板库、模板同步、复杂脚本变量或 Notion 式数据库模板。

内置模板优先覆盖常见写作场景：会议纪要、PRD、技术方案、周报、公众号长文、学术笔记。入口为斜杠菜单 `/template` 与命令面板 `插入模板`。选择模板后把普通 Markdown 内容插入当前光标位置；如果当前文档为空，也可以作为整篇文档模板。

模板内容可以包含 YAML Front Matter、标题、列表、Callout、Mermaid 占位等标准 Markdown/HTML。第一版最多支持简单占位符：`{{date}}`、`{{title}}`、`{{author}}`。

### 页面链接、反向链接与关系图谱
页面链接服务工作区内 Markdown 文档跳转。输入 `[[` 触发文件/标题搜索，可搜索当前工作区 Markdown 文件名与文档内标题。插入结果优先使用普通 Markdown 链接而不是私有 wiki link 存储：文件链接为 `[文档名](relative/path.md)`，标题链接为 `[标题](relative/path.md#heading)`。已存在的 `[[文档名]]` 可以在预览中识别并跳转，但保存时不强制改写。

Backlinks 通过扫描工作区 Markdown 链接生成，入口第一版放命令面板 `查看反向链接`，不放状态栏。

Prism 可以提供轻量文档关系图谱，但不做 Obsidian 式重型知识宇宙图。图谱入口为命令面板 `查看关系图谱`，默认以当前文档为中心展示 1-2 跳关系；节点是 Markdown 文件，边是链接/反向链接；点击节点直接打开对应文档；支持搜索节点，并支持 `当前文档` 与 `当前工作区` 两种范围。不做标签宇宙图、块级节点、数据库属性关系、3D 图谱、常驻右栏或插件化图谱生态。

### Front Matter 属性面板
YAML Front Matter 可视化属性面板可以做，但保持轻量，只服务写作、搜索、导出和模板。第一版字段包括：`title`、`tags`、`description`、`author`、`date`、`status`、`export`。

入口第一版为命令面板 `打开文档属性`；如果后续被证明高频，再考虑固定入口。面板可视化编辑字段，保存时写回文档顶部 YAML Front Matter。`export` 字段可服务导出模板，例如默认主题、是否显示目录、纸张尺寸等。

属性面板不是 Notion database properties，不支持 relation、rollup、formula、多类型复杂字段或数据库视图。

### 块级操作（Block operations）
Prism 不做完整 block editor，只做基于 Markdown 文本范围的当前段落/当前标题章节操作。源码仍然可见、可编辑，操作本质是对 Markdown 文本范围的编辑。

第一版操作包括：上移当前段落、下移当前段落、复制当前段落、删除当前段落、把选区转成引用、把选区转成 Callout、把选区转成任务列表、折叠当前标题章节、复制当前标题章节。

入口为命令面板与右键菜单。后续可评估行首轻量 handle，但第一版不做常驻拖拽把手、不做 Notion 式块数据库、不做整篇文档拖拽重排、不隐藏 Markdown 源码。

### 快速打开与工作区搜索
快速打开与搜索属于写作导航增强，不是 IDE 搜索中心。第一版支持 `Cmd+P` 快速打开当前工作区 Markdown 文件，`Cmd+Shift+F` 全文搜索当前工作区 Markdown，命令面板显示最近打开文档。搜索结果点击直接打开文档。

搜索支持文件名、标题、路径的模糊匹配；不实现 IDE 式复杂 search/replace 全套，不索引二进制文件，不做云端搜索。该能力应与 `[[` 页面链接搜索、backlinks、关系图谱共用同一套工作区索引。

### 导出保真与稳定
导出能力继续作为重点优化方向，但目标是保真、稳定和可诊断，不是继续堆更多格式。HTML/PDF/PNG/DOCX 输出应尽量接近 Prism 预览，尤其是 Mermaid、KaTeX、SVG、本地图片、Callout、Toggle 等非纯文本内容。

导出应避免图片或大型渲染块被分页切断；PDF/PNG 默认高清，不自动降清晰度。若导出可能耗时较长，应在开始前给出清晰提示，并支持后台任务状态。DOCX 对复杂非文字内容可以采用图片化嵌入，优先保证视觉结果接近预览。

导出失败必须提供可理解诊断，包括阶段、格式、文件路径、可能原因和下一步建议，而不是只显示一行底层错误。不新增冷门格式、云端导出、协作审阅或 LaTeX 出版级排版全套。

### AI 能力边界
AI 暂不纳入当前核心实现，避免把 Prism 从可靠的本地 Markdown 写作器带偏成 AI 写作平台。模板、导出、搜索、链接索引等核心能力不得依赖 AI。

可以为未来预留命令面板分组或架构边界，但默认不实现 AI 自动写作、AI 改写、AI 总结、聊天侧栏、agent 工作流或云端知识库问答。后续若实现 AI，只做本地可控的选区级辅助，例如改写选区、总结当前章节、提取标题、生成摘要。

### 插件与扩展边界
当前不做插件系统、插件市场、第三方扩展 API、脚本执行或主题市场。对外扩展先通过配置、模板和内置命令解决，不开放执行型插件能力。

内部可以保留 extension registry 或 adapter registry，用于组织内置 Markdown 扩展、预览渲染器、导出适配器等能力，但这不是对外插件平台。

### 本地优先边界
当前不做移动端、账号系统、云同步、实时协作、评论审阅流或团队空间。Prism 继续保持本地优先、单文档写作器定位。

本地安全感通过更可靠的自动保存、保存失败诊断、恢复快照、文件冲突提示和最近文件恢复来增强，而不是通过云端能力解决。

### 优化实施顺序
这批产品优化按“低破坏、高收益、打基础”的顺序推进，不一次性重写体验：

1. 状态栏降噪
2. 命令面板整理
3. 斜杠菜单
4. Callout / Toggle 渲染与导出
5. 工作区索引
6. 快速打开 / 全文搜索 / 最近文档
7. `[[` 页面链接与反链
8. 轻量关系图谱
9. Front Matter 属性面板
10. 模板系统
11. 块级操作
12. 导出保真与稳定专项

每一阶段都应保持增量开发：不破坏现有视觉、不移除已确认保留的入口、不引入与单文档本地写作器定位冲突的能力。

每一阶段必须同时满足四个完成标准：功能可用、视觉不破坏、已有功能不回退、有最小验证。功能可用指用户能在真实 app 里完成核心动作；视觉不破坏指 UI 符合当前 Prism 妙言风格；已有功能不回退指编辑、预览、保存、导出、快捷键、文件树等既有链路不受影响；最小验证按风险分层，小 UI 改动跑相关测试与人工 smoke，涉及导出、索引、文件系统的阶段跑更完整验证。

### 令牌体系（Design tokens）
颜色命名直接沿用原型：`--c-void` / `--c-canvas` / `--c-fog` / `--c-chalk` / `--c-graphite` / `--c-ash` / `--c-hair` / `--c-hover` / `--c-selection`。不再使用 Fluent 式 `--bg-*` / `--text-*` / `--accent-*` 命名。详见 ADR-0002。
