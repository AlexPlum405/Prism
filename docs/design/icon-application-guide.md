# Prism 图标应用摘要

最终图标源文件保留为 `docs/assets/prism-icon-red-panda-app-icon.png`，打包输出保留在 `src-tauri/icons/`；根目录中的候选图、备份图和裁切过程图均已清理，不再作为发布资产保存。

后续更换图标只需要替换源图并运行 `node scripts/generate-brand-icons.mjs`，macOS 文档图标和默认打开方式验证记录见 `docs/releases/prism-macos-1.0.0-confidence-pack/evidence/app-identity-and-document-icons.md`。
