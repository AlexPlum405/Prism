# Promo Page Assets

`promo-page/index.html` currently references real still screenshots from `../screenshots/` directly, so this folder is reserved for generated GIF/video source files. Playwright QA screenshots were generated once to verify desktop/mobile rendering, then removed from the repository to keep the release pack small.

Pending motion assets:

- `hero-writing-preview.mp4`
- `diagram-export-flow.mp4`
- `diagnostics-fix-loop.mp4`
- `finder-open-file.mp4`
